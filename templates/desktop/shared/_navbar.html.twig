<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="position: fixed; width: 100%; z-index: 10;">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? "") == "recipe") ? "active" : "" }}" href="{{ path("recipe_index") }}">Recettes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? "") == "booklet") ? "active" : "" }}" href="{{ path("booklet_index") }}">Mon Carnet</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? "") == "department") ? "active" : "" }}" href="{{ path("department_index") }}">Rayons</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? "") == "shoppingList") ? "active" : "" }}" href="{{ path("shopping_list_index") }}">Listes</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item"><a href="{{ path("security_logout") }}" class="btn btn-danger">Logout</a></li>
                {% else %}
                    <li class="nav-item"><a href="" class="nav-link">Inscription</a></li>
                    <li class="nav-item"><a href="{{ path("security_login") }}" class="btn btn-success">Login</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
